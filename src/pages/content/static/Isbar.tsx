import { projectPageContent } from "../../../components/automated/baseProjectTypes";
import { useTheme } from "@mui/material";

export default function Isbar(prop: { content: projectPageContent }) {
    const theme = useTheme();

    return (
        <>

        </>
    )
}